version: '3'

services:
  web:
    container_name: django-web-app
    restart: always
    image: silica-test-app
    working_dir: /app
    command: python manage.py runserver 0.0.0.0:8000
    environment:
      - DEBUG=True
    volumes:
      - ../example_project:/app
    ports:
      - 8000:8000
