[tool.poetry]
name = "pytarantula"
version = "0.1.2"
description = "Python client for tarantula service."
authors = ["pierre steen <pierre@84a.dev>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"

[tool.poetry.group.dev.dependencies]
grpcio = "^1.59.0"
build = {version = "^1.0.3", allow-prereleases = true}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
