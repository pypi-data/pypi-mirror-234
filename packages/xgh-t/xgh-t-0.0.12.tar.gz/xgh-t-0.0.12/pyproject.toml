[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "xgh-t"
version = "0.0.12"

[project.scripts]
x-t = "t:main"

[tool.setuptools.packages.find]
where = ["src"]