[tool.poetry]
name = "cdk-deploy"
version = "0.1.0"
description = ""
authors = ["fluxionality"]
readme = "README.md"
packages = [{include = "cdk_deploy"}]

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
