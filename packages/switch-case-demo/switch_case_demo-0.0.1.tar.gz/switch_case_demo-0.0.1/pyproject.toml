[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "switch_case_demo"
description = "A sample python package to implement switch case in python"
version = "0.0.1"
readme = "README.md"
requires-python = ">=3.7"
dependencies = []