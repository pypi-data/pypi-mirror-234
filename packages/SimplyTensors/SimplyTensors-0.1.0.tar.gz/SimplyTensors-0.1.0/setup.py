from setuptools import setup

setup(
    name='SimplyTensors',
    version='0.1.0',
    description='A simple AI module for working with tensors',
    author='Your Name',
    py_modules=['st'],
    install_requires=[
        'numpy',
        # Add other dependencies as needed for your AI module
    ],
)
