image: paternal/pythons

before_script:
  - apt update && apt install --yes --no-install-recommends
    libdbus-1-dev
    libglib2.0-dev

tox:
  script:
    - python3 -m tox
  coverage: '/^TOTAL.*\s(\d+)\%$/'
