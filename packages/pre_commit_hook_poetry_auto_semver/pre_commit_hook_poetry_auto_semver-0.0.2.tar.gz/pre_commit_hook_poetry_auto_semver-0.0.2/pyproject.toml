[tool.poetry]
name = "pre_commit_hook_poetry_auto_semver"
version = "0.0.2"
description = ""
authors = ["Ian Pearthree <52967928+ipear3@users.noreply.github.com>"]
readme = "README.md"
packages = [
    { include = "pre_commit_hook_poetry_auto_semver" }
]

[tool.poetry.dependencies]
python = "^3.11"
semver = "^3.0.1"
pre-commit = "^3.4.0"

[tool.poetry.scripts]
poetry-auto-semver = "pre_commit_hook_poetry_auto_semver.poetry_auto_semver:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
