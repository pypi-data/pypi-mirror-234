[tool.poetry]
name = "pre-commit-hook-poetry-auto-semver"
version = "0.0.1"
description = ""
authors = ["Ian Pearthree <52967928+ipear3@users.noreply.github.com>"]
readme = "README.md"
packages = [
    { include = "pre-commit-hook-poetry-auto-semver" }
]

[tool.poetry.dependencies]
python = "^3.11"
semver = "^3.0.1"
pre-commit = "^3.4.0"

[tool.poetry.scripts]
poetry-auto-semver = "pre-commit-hook-poetry-auto-semver.poetry-auto-semver:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
