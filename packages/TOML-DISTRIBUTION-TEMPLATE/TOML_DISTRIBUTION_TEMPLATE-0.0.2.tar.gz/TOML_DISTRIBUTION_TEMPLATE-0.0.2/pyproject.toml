[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "TOML_DISTRIBUTION_TEMPLATE"
version = "0.0.2"
description = ""
readme = ""

[tool.setuptools.packages.find]
where = ["src"]