[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "afex-devtools"
version = "0.32"
requires-python = ">=3.9"
dependencies = [
  "grpcio-tools==1.50.0",
  "Django==4.1.6",
  "djangorestframework==3.14.0",
  "any-case==0.1.6"
]
