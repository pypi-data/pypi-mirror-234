# setup.py
from setuptools import setup, find_packages

setup(
    name='mis_test_package',
    version='0.0.4',
    packages=find_packages(),
    install_requires=[
        # add any dependencies here
    ],
)