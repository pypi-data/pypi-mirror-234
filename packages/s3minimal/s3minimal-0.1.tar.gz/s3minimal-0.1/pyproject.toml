[tool.poetry]
name = "s3minimal"
version = "0.1"
description = ""
authors = ["Jamal <jsaied99@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
aiobotocore = { extras = ["boto3"], version = "^2.6.0" }


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
