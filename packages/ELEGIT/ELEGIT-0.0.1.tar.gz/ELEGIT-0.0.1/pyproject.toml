


#
#	HOW TO PUBLISH:
#
#		(rm -rf dist && python3 -m build --sdist && twine upload dist/*)
#		(git add --all && git commit -m "com" && git push --set-upstream git@gitlab.com:reptilian_climates/ELEGIT.git CLIMATE)
#

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ELEGIT"
version = "0.0.1"
description = "ELE(GIT)"
readme = "STRUCTURE.rst"

[tool.setuptools.packages.find]
where = [ "CALCULATOR/STRUCTURES" ]

#
#	https://setuptools.pypa.io/en/latest/userguide/datafiles.html
#
[tool.setuptools.package-data]
"*" = [ "*.HTML" ]