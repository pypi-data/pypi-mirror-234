[build-system]
requires      = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "espressopy"
version = "0.0.1"
description = "The new way to create Discord bots."
readme = "README.md"
dependencies = [
    "websockets",
    "asyncio"
]