[tool.poetry]
name = "realms"
version = "0.1.0"
description = "An simple, powerful Python game engine"
authors = ["Dylan Rogers <opendylan@proton.me>"]
license = "GPL-3.0-or-later"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3"
pygame = "^2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
